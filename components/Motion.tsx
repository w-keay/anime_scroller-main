"use client";

/**
 * MotionDiv component for the Anime Scroller web application.
 * A styled motion.div component from the framer-motion library for animated transitions.
 */

// Importing motion.div from the framer-motion library
import { motion } from "framer-motion";

// Exporting the styled motion.div component for use in animations
export const MotionDiv = motion.div;
